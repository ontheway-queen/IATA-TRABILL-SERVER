import { ICompanyContactPerson } from '../types/client.interfaces';

class clientsHelpers {
  /**
   * Prses company contact person to array of objects
   *
   * @param reqPersonInfo company contact person information from request body
   * @param company_id the company that the person belongs to
   * @returns array of comapany contact person object
   */
}

export default clientsHelpers;
